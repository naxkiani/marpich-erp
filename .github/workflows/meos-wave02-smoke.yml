name: MEOS Wave 02 Smoke

on:
  push:
    branches: [main, "feature/**"]
  pull_request:
    branches: [main]

jobs:
  backend-wave02-memory:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install backend
        run: |
          python -m pip install -U pip
          pip install -e ".[dev]" || pip install -e .
          pip install pytest httpx
      - name: Wave 02 Q2C pytest chain
        env:
          PERSISTENCE_BACKEND: memory
        run: |
          pytest -q \
            contexts/crm/tests \
            contexts/sales/tests \
            contexts/inventory/tests \
            contexts/accounting/tests/test_ar_invoice_flow.py \
            contexts/procurement/tests/test_procurement_flow.py \
            contexts/human_resources/tests/test_hr_employee_flow.py \
            contexts/payroll/tests/test_payroll_flow.py \
            --maxfail=5 \
            -k "not postgres"

  backend-wave02-q2c-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: marpich
          POSTGRES_PASSWORD: marpich
          POSTGRES_DB: marpich_platform
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U marpich -d marpich_platform"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    defaults:
      run:
        working-directory: backend
    env:
      PERSISTENCE_BACKEND: postgres
      DATABASE_URL: postgresql+asyncpg://marpich:marpich@127.0.0.1:5432/marpich_platform
      JWT_SECRET: ci-wave02-jwt-secret-not-for-prod
      PGHOST: 127.0.0.1
      PGPORT: "5432"
      PGUSER: marpich
      PGPASSWORD: marpich
      PGDATABASE: marpich_platform
      API_URL: http://127.0.0.1:8000
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install backend + psql client
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
          python -m pip install -U pip
          pip install -e ".[dev]" || pip install -e .
          pip install uvicorn httpx asyncpg
      - name: Apply migrations through Wave 02
        working-directory: .
        run: ./scripts/run-migrations.sh
      - name: Start API
        run: |
          nohup uvicorn core.presentation.api.main:app --host 127.0.0.1 --port 8000 \
            > /tmp/marpich-wave02-api.log 2>&1 &
          echo $! > /tmp/marpich-wave02-api.pid
          for i in $(seq 1 60); do
            if curl -sf http://127.0.0.1:8000/api/v1/health >/dev/null; then
              echo "API ready"
              exit 0
            fi
            sleep 2
          done
          echo "API failed to start" >&2
          tail -100 /tmp/marpich-wave02-api.log >&2 || true
          exit 1
      - name: Wave 02 Q2C closed-loop script
        working-directory: .
        run: |
          chmod +x ./scripts/meos-wave02-q2c-loop.sh
          ./scripts/meos-wave02-q2c-loop.sh
      - name: API logs on failure
        if: failure()
        run: tail -200 /tmp/marpich-wave02-api.log || true
