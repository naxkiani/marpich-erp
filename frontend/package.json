{
  "name": "marpich-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Marpich ERP — Next.js modular frontend",
  "scripts": {
    "dev": "pnpm --filter @marpich/admin-portal dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
