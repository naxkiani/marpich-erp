# Orphan API clients (P0 honesty)

These modules had **no page imports** in admin_portal. They are gated here until Wave 02+
ships matching Functional desks.

Do **not** re-export from active `src/lib/` until a route + AuthZ-wired UI exists.
