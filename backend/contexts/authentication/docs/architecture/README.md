# WebAuthn / Passkey UI surface (P30)

**Owning context:** `authentication`  
**Admin desk:** `/account/passkeys` → `AccountPasskeysPage`

Operational routes under `/api/v1/authentication/webauthn*`.  
Client helpers live in `@marpich/auth-provider` (`registerPasskey`, `listPasskeys`, `revokePasskey`).

See also: `contexts/identity/docs/architecture/README.md` (account security hub).
