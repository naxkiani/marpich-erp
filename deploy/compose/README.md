# Compose adapters (LOCAL / DEMO — COMPOSE_IS_PRODUCTION = FALSE)

| Profile | File |
|---------|------|
| LOCAL | `infrastructure/docker/compose/docker-compose.dev.yml` |
| DEMO | `infrastructure/docker/compose/docker-compose.meos-prod.yml` |

One-command wrappers: `../scripts/` (start/stop/reset/backup/restore/health).
