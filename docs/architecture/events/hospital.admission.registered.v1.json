{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hospital.admission.registered.v1",
  "title": "hospital.admission.registered.v1",
  "type": "object",
  "required": ["admission_id", "patient_id"],
  "properties": {
    "admission_id": { "type": "string", "format": "uuid" },
    "patient_id": { "type": "string", "format": "uuid" },
    "ward": { "type": ["string", "null"], "minLength": 1 }
  },
  "additionalProperties": false
}
