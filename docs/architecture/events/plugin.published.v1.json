{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "marpich.plugin.published.v1",
  "title": "plugin.published",
  "allOf": [
    { "$ref": "_envelope.v1.json" },
    {
      "properties": {
        "event_name": { "const": "plugin.published" },
        "event_version": { "const": 1 },
        "source_context": { "const": "plugins" },
        "payload": {
          "type": "object",
          "required": ["plugin_id", "version", "trust_level"],
          "properties": {
            "plugin_id": { "type": "string", "minLength": 1 },
            "version": { "type": "string", "minLength": 1 },
            "trust_level": { "type": "string", "minLength": 1 }
          }
        }
      }
    }
  ]
}
