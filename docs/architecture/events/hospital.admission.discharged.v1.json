{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hospital.admission.discharged.v1",
  "title": "hospital.admission.discharged.v1",
  "type": "object",
  "required": ["admission_id", "patient_id", "ward"],
  "properties": {
    "admission_id": { "type": "string", "format": "uuid" },
    "patient_id": { "type": "string", "format": "uuid" },
    "ward": { "type": "string", "minLength": 1 },
    "bed_id": { "type": ["string", "null"], "format": "uuid" }
  },
  "additionalProperties": false
}
