{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "marpich.plugin.upgraded.v1",
  "title": "plugin.upgraded",
  "allOf": [
    { "$ref": "_envelope.v1.json" },
    {
      "properties": {
        "event_name": { "const": "plugin.upgraded" },
        "event_version": { "const": 1 },
        "source_context": { "const": "plugins" },
        "payload": {
          "type": "object",
          "required": ["plugin_id", "from_version", "to_version"],
          "properties": {
            "plugin_id": { "type": "string", "minLength": 1 },
            "from_version": { "type": "string", "minLength": 1 },
            "to_version": { "type": "string", "minLength": 1 }
          }
        }
      }
    }
  ]
}
