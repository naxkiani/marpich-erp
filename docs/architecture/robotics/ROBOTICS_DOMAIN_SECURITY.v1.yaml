# ROBOTICS_DOMAIN_SECURITY.v1.yaml — P216-C
apiVersion: marpich.io/v1
kind: RoboticsDomainSecurityCatalog
metadata:
  phase: P216-C
  adr: "475"
spec:
  principles:
    - zero_trust_robot_identity
    - command_authorization_per_mission
    - safety_gated_autonomy
    - physical_ai_via_p214z_acl_only
  hard_laws:
    - never_replace_p216_foundation
    - never_replace_p216_a_mission
    - never_replace_p216_b_strategy
    - never_peer_domain_imports
    - never_cross_context_aggregate_mutation
    - ungated_physical_autonomy_forbidden
    - opaque_safety_forbidden
